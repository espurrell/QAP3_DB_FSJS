<!DOCTYPE html>
<html lang="en">
<head>
    <title>Martha's Good Eats - Products </title>
</head>
<body>
    <h1>Product List</h1>
    <ul>
        <% for(let product of products) { %>
            <li><%= product.name %> - $<%= product.price %></li>
        <% } %>
    </ul>
</body>
</html>